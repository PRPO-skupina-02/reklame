apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reklame
  namespace: production
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  tls:
  - hosts:
    - reklame.cinecore.timhrovat.com
    secretName: reklame-tls
  rules:
  - host: reklame.cinecore.timhrovat.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reklame
            port:
              number: 8080
